# vim: set ft=sh:
# shellcheck shell=sh
#
# false positive
# shellcheck disable=2154
#
# run_keymap()
{
    [ "$break" = keymap ] && { print "break before run_keymap()"; sh; }

    loadkmap < "$keymap_path"
}
